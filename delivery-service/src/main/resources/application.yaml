server:
  port: 19099

spring:
  application:
    name: delivery-service
  config:
    import:
      - "optional:file:.env[.properties]"
      - "optional:file:../.env[.properties]"
      - "optional:configserver:${CONFIG_SERVER_URL:http://localhost:19092}"