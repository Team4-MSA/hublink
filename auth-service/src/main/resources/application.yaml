spring:
  application:
    name: auth-service

  config:
    import: "optional:configserver:http://localhost:19095"

eureka:
  client:
    service-url:
      defaultZone: https//localhost:19090/eureka/